# deskmind

deskmind 是一款 Windows 桌面软件，用 AI 把用户的启动项分好组，并把这些分组直接画在桌面上。它只有一个窗口，压在 z-order 最底——用户看向桌面时看到的就是它（见 ADR 0017）。**整理**是它的核心能力。

deskmind 不替换 explorer.exe，也不移动、重命名或删除用户的任何文件。

## Language

**启动项 (Launch Target)**:
可以被启动的对象——已安装的程序、桌面上的文件与文件夹。来源是开始菜单快捷方式目录与桌面文件夹。
_Avoid_: 应用、程序、条目、桌面项

**分区 (Zone)**:
启动项的一个具名分组。分区由用户拥有——创建、命名、删除的最终决定权在用户，AI 只能提出建议。

分区**不是文件夹**。把一个启动项放进分区只改变归属，磁盘上的文件原地不动。从资源管理器把文件拖进来同理：那是**新增一个启动项**（只记路径），不是把文件收进某个目录。
_Avoid_: 围栏、收纳盒、分组框、标签

**整理 (Tidy)**:
由 AI 判断每个启动项应归入哪个**已有**分区。整理只改变分组归属，从不移动或修改磁盘上的文件，也不会自行新建或删除分区。
_Avoid_: 分类、归类、清理

**分区建议 (Zone Suggestion)**:
首次运行时 AI 根据用户的启动项提出的一套分区方案。用户可以整体接受、逐个修改或全部拒绝。这是 AI 唯一能创建分区的场合。
_Avoid_: 自动分区、智能分组

**磁贴 (Tile)**:
启动项在桌面网格中的视觉呈现。磁贴由 deskmind 自己绘制，外观完全可控——这与系统原生桌面图标不同，后者的外观不可改变。
_Avoid_: 卡片、图标

**未分类 (Unzoned)**:
还没有归入任何分区的启动项。未分类**不是分区**——AI 建不出它，用户删不掉它，它不计入分区数量；它只是「没有分区」这个状态的名字。名叫「其他」的分区如果存在，那就是一个普通分区，与未分类无关。
_Avoid_: 默认分区、待整理、其他

**隐藏 (Hidden)**:
用户把某个启动项从桌面网格里拿掉，包括「全部」。隐藏只决定桌面上看不看得见：搜索照样搜得到、启动得了，磁盘上的文件也原地不动。
_Avoid_: 删除、移除、归档

**固定 (Pin)**:
用户把某个启动项标记为常用，使其在所属分区内优先呈现。
_Avoid_: 收藏、置顶

**主题 (Theme)**:
壁纸与由它派生出的界面配色。只描述外观，不包含布局。
_Avoid_: 皮肤、外观
