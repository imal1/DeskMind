# deskmind 设计规格

> **Status**: 本文档是 v2「桌面分区」的设计输入，**不适用于 v1**。
>
> v1 的设计已完成，见 [`docs/design/`](./docs/design/)，形态是全屏**启动台**（见 ADR 0013）。本文档描述的是桌面上的半透明分区卡片，那部分推迟到 v2。
>
> 第 2 节的技术约束（图标外观不可控、受桌面图标网格约束、分区不能折叠）在 v2 依然成立，届时仍需遵守。**这些约束不适用于 v1 的磁贴**——磁贴由 deskmind 自绘，外观完全自由。

这份文档是给设计工作用的输入。它描述**要设计什么、有哪些状态、以及哪些约束不可协商**，不规定具体的视觉风格细节——那是设计要解决的部分。

术语一律以 [CONTEXT.md](./CONTEXT.md) 为准。决策背景见 `docs/adr/`。

---

## 1. 产品是什么

deskmind 是一款 Windows 桌面美化与启动器软件。核心体验是一句话：**点一下，桌面自己整齐了。**

它在桌面上绘制**分区**，用 AI 判断每个**桌面项**该归入哪个分区，然后把图标移进去。它不替换 explorer.exe，也不移动用户的任何文件。

目标用户是愿意折腾的发烧友，通过 GitHub 分发，MIT 许可。

---

## 2. 不可协商的技术约束

设计必须在这些约束内进行。违反它们的方案做不出来。

### 2.1 图标不归我们画

分区里的图标是 Windows 自己的桌面图标。deskmind 只能**移动它们的坐标**，不能改变它们的外观、大小、间距或排列方式（见 ADR 0008）。

这意味着：

- **图标位置受 Windows 桌面图标网格约束**。网格尺寸由系统的图标大小设置决定，随用户设置变化。分区的尺寸与内部留白必须按网格的整数倍来设计，不能自由排布
- 不能给图标加角标、加边框、换样式、做悬停效果
- 图标的标签文字（文件名）样式由系统控制，我们改不了
- 图标的选中态、拖拽态、右键菜单都是系统的

**设计时请把图标当成一张尺寸固定、外观不可控的贴纸。** 可设计的只有它周围的容器。

### 2.2 分区不能折叠

v1 没有办法隐藏单个原生图标，所以分区不能折叠、不能滚动、不能有"超出部分收起"。分区必须大到能装下它所有的图标。

### 2.3 配色由壁纸决定

分区的文字色和半透明底色由当前壁纸的明暗与主色算出，不跟随系统深浅色模式（见 ADR 0012）。

设计需要给出的是**规则**，不是固定色值：亮壁纸下用什么、暗壁纸下用什么、底色如何从壁纸主色派生。请确保方案在极端情况下仍可读——纯白壁纸、纯黑壁纸、高饱和度壁纸、以及明暗对半的壁纸。

### 2.4 没有主窗口

所有界面要么画在桌面上，要么是按需唤出的浮层（见 ADR 0010）。不要设计带导航栏、侧边栏或多页面的应用窗口。

---

## 3. 视觉方向

已定：**分区是半透明卡片**——有明确边界，同时让壁纸透出来。

选它的理由是同时满足两件事：用户必须看得出桌面项被归好了（所以需要边界），而壁纸必须继续可见（这是美化软件的根本）。半透明底色取自壁纸主色，让分区看起来长在壁纸上，而不是贴在上面。

被否的方向：纯描边（太弱，看不出产品存在感）、实色面板（盖住壁纸，与美化定位冲突）。

---

## 4. 要设计的表面

一共六个。

### 4.1 分区（桌面上）

用户 90% 的时间看到的东西。其余所有界面的视觉气质都跟着它走。

**元素**：标题、容纳图标的区域、尺寸调整手柄、悬停时出现的操作入口（重命名、删除）。

**状态**：

| 状态 | 说明 |
|---|---|
| 默认 | 静置，尽量不打扰壁纸 |
| 悬停 | 出现重命名、删除入口 |
| 标题编辑中 | 标题变为可输入 |
| 拖拽目标 | 用户拖着图标经过时，需要明确的"可以放这里"反馈 |
| 空分区 | 刚建好还没有内容。需要提示但不能喧宾夺主 |
| 整理中 | 正在接收飞过来的图标 |

### 4.2 托盘图标与菜单

**菜单项**：整理、搜索、新建分区、设置、退出。

**状态**：空闲、整理中（需要一个进行中的指示）。

### 4.3 整理结果提示条

整理完成后出现的短暂提示，几秒后自动淡出。

**必须在视线内，不能藏进托盘。**

**状态**：

| 状态 | 内容 |
|---|---|
| 整理中 | 进行中指示 |
| 完成 | 「已整理 23 项」+ 撤销入口 |
| 部分完成 | 有些项 AI 无法归类，说明有几项留在原处 |
| 失败 | API 出错。说明发生了什么、下一步做什么，不要暴露原始报错 |

### 4.4 搜索浮层

热键唤出，居中。索引来源是桌面项与开始菜单快捷方式（即**启动项**）。

**状态**：刚唤出（空）、输入中有结果、无结果、结果项选中。

### 4.5 设置浮层

**内容**：API key、模型选择、开机自启、热键设置。

**状态**：key 未填（首次，需要引导用户去哪拿 key）、已填、连接测试中、测试失败。

### 4.6 首次运行

用户第一次打开时的完整流程，也是产品的第一印象。

**步骤**：欢迎与填 key → AI 分析桌面中 → 展示**分区建议** → 用户接受/改名/删除 → 完成。

分区建议这一屏是唯一一次 AI 可以创建分区的场合（见 ADR 0009），设计上要让「这是建议、你可以改」这件事显而易见。

---

## 5. 核心交互流程

### 5.1 整理（最重要的一段）

用户从托盘菜单或热键触发 → 图标**飞向**各自的分区 → 提示条出现，显示数量与撤销入口 → 几秒后淡出。

**没有确认步骤。** 这是刻意的：核心体验是"点一下桌面自己整齐了"，在这个瞬间前插一道确认关卡会把它消解成"我又得审一遍"。之所以敢这么做，是因为整理不动任何文件（见 ADR 0004），全部后果只是一组坐标变化，撤销就是把坐标写回去。

**图标必须飞过去，不能瞬移。** 那段动画是整个产品的记忆点，也是用户理解"刚才发生了什么"的唯一线索。瞬移会让人以为桌面出了 bug。

需要设计：动画时长与缓动曲线、多个图标同时移动时的编排（是否错峰）、以及数量很多时（比如 80 项）如何不显得混乱。

### 5.2 搜索

热键唤出 → 输入 → 上下键选择 → 回车启动 → 浮层关闭。Esc 随时关闭。

### 5.3 撤销

提示条上的撤销入口点击后，所有图标回到整理前的位置。同样要有动画。

---

## 6. 明确不要设计的东西

- 应用主窗口、导航栏、侧边栏、设置页面
- 分区的折叠、展开、滚动、嵌套
- 任何改变图标本身外观的效果
- 壁纸浏览器、壁纸库、动态壁纸控制面板
- 对话框式的 AI 聊天界面（v1 的 AI 没有对话，只有一次整理）
- 场景切换界面（术语已定义，但 v1 不实现）
- 插件市场、账号体系、引导性弹窗

---

## 7. 需要设计给出答案的开放项

1. 分区在极端壁纸下的可读性方案（纯白、纯黑、高饱和、明暗对半）
2. 分区数量很多时（10 个以上）桌面的整体观感如何不崩
3. 整理动画在 80 项规模下的编排
4. 空分区的提示方式——既要说明白，又不能在有多个空分区时形成视觉噪音
5. 托盘图标在深色与浅色任务栏下的辨识度
6. 提示条的位置：屏幕顶部居中、底部居中，还是靠近托盘

---

## 8. 语气

界面文案用中文，简短、直白、不客套。

不写「成功」（成功提示本身就是成功）、不写「请」、不用感叹号。错误文案先说发生了什么，再说下一步做什么，不暴露原始报错。
